apiVersion: v1
kind: Service
metadata:
  name: eyeflow-web
  namespace: eyeflow
spec:
  selector:
    app.kubernetes.io/name: eyeflow-web
  ports:
    - name: http
      port: 80
      targetPort: http
